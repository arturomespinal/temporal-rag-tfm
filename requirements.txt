# ============================================================
# Temporal RAG — TFM Máster en Ciencia de Datos
# https://github.com/arturomespinal/temporal-rag-tfm
# ============================================================
# Instalar con: pip install -r requirements.txt
# Versiones mínimas probadas con Python 3.10+

# ── Deep Learning ────────────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0          # Solo necesario si se usan utilidades de visión

# ── Indexación vectorial (§6.4) ───────────────────────────────
faiss-cpu>=1.7.4             # CPU-only. Para GPU: pip install faiss-gpu
# faiss-gpu>=1.7.4           # Descomentar si hay CUDA disponible

# ── Datos y análisis ──────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0

# ── Series temporales — descomposición y benchmarking ─────────
statsmodels>=0.14.0          # STL, ACF/PACF, ADF (§5.3.2–5.3.3)

# ── Métricas de evaluación ────────────────────────────────────
scikit-learn>=1.3.0          # F1, AUROC, Precision, Recall (§7.2.3)

# ── Visualización (EDA — eda.py) ──────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0              # Opcional: heatmaps adicionales

# ── Benchmarking de anomalías ─────────────────────────────────
pyod>=1.1.0                  # Baselines: USAD, IsolationForest (§6.8)

# ── Foundation models (extensiones opcionales — §6.8) ─────────
# chronos-forecasting>=1.0   # Amazon Chronos (embeddings pre-entrenados)
# timesfm>=1.0               # Google TimesFM

# ── Utilidades ────────────────────────────────────────────────
tqdm>=4.65.0                 # Barras de progreso
